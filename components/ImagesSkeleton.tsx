import { Skeleton } from "./ui/skeleton";

const ImagesSkeleton = () => {
  return (
    <Skeleton className="absolute inset-0 w-full h-full flex flex-col items-center dark:text-background justify-center text-primary-foreground uppercase font-bold text-2xl">
      lOKKO
    </Skeleton>
  );
};

export default ImagesSkeleton;
